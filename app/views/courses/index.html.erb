 

<h1>Courses</h1>

<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Show this course", course %>
    </p>
  <% end %>
</div>

<%= link_to 'Home', home_url %> <%= link_to "New course", new_course_path if policy(:course).new? %>
