 

<%= render @course %>

<div>
  <%= link_to "Edit this course", edit_course_path(@course) if policy(:course).edit? %> <%= link_to "Back to courses", courses_path if policy(:course).index? %>

  <%= button_to "Destroy this course", @course, method: :delete if policy(:course).destroy?%>
</div>
